[project]
name = "lerobot-zmq-pi-side"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyzmq>=26.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
